export interface Product {
    id: number;
    name: string;
    quantity: number;
}

export interface Comment {
    id: number;
    name: string;
    email: string;
    rating: number;
    image: string;
    message: string;
    date: string;
}

